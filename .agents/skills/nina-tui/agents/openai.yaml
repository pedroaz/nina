interface:
  display_name: "Nina TUI"
  short_description: "Nina TUI screens and settings editing"
  default_prompt: "Use $nina-tui to work on Nina's TUI navigation, screen behavior, and settings editing."
